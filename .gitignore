node_modules/
dist/
.cache/
*.log
.DS_Store

# env / local overrides
.env
.env.*
!.env.example
# Do not commit machine-local auth tokens. Scoped registry for @stockheartbeat is in .npmrc.
# If you add _authToken to .npmrc locally, use `git update-index --skip-worktree .npmrc`.

# editor / OS
.vscode/
.idea/

# test artifacts
coverage/
